{
  "extends": "@tsconfig/react-native/tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./src", // all paths are relative to the baseUrl
    "paths": {
      "@src/*": ["src/*"],
      "tests": ["tests/*"],
    },
  }
}
